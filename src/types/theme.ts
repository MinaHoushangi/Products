interface MyTheme {
  colors: {
    accent: string;
    primary: string;
    primaryLight: string;
    background: string;
    text: string;
    border: string;
    textDark: string;
    lightGrayBackground: string;
  };
}

export type {MyTheme};
